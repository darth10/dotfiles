#!/bin/bash

process() {
    while read input; do
        echo "event:" $input
        case "$input" in
            UNBLANK*)
                xkbset sticky -twokey latchlock
                ;;
            LOCK*)  ;;
        esac

        sleep 1
    done
}

xscreensaver-command -watch | process
